#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=sort-tabs
CHROME_PROVIDERS=""
CLEAN_UP=1
ROOT_FILES="chrome.manifest"
ROOT_DIRS=
BEFORE_BUILD=
AFTER_BUILD=
